# A14 — Stale schema-3 two-photon test runner

Audit finding: `TwoPhotonTestRunnerApp` is stale or broken relative to protocol schema 3. Compare it with the current schema-3 resolver, unified workflow, supported standalone commissioning behavior, and simulated launcher. Restore the standalone test runner using existing schema-3 interfaces without changing experimental policy or reintroducing obsolete schema behavior.

Focused test:

```text
matlab -nodisplay -nosplash -batch "cd(pwd); results = runtests('tests/TestAdaptiveOptopatch.m'); assertSuccess(results)"
```

Likely touch area: `+adaptive_optopatch/TwoPhotonTestRunnerApp.m`, its launchers, and focused tests.
