# C2 — Frozen manifest mutation in staged 2P runs

Audit finding: the 2P runner rewrites a frozen manifest under release-level staging. Preserve the archived manifest as immutable acquisition truth while still supporting the intended staged execution behavior. Trace `run_2p_manifest`, release-level validation, resume/checkpoint behavior, and relevant tests before choosing the smallest correction.

Focused test:

```text
matlab -nodisplay -nosplash -batch "cd(pwd); results = runtests('tests/TestAdaptiveOptopatch.m'); assertSuccess(results)"
```

Likely touch area: `+adaptive_optopatch/run_2p_manifest.m` and focused tests. Do not address C3's attenuation semantics in this task except where an inseparable interface change is required; document any such overlap.
