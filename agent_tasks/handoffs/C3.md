# C3 — Attenuated test does not attenuate execution

Audit finding: `attenuated_test` requires an override but does not actually attenuate the command that reaches hardware. Starting from `main` after C2 is integrated, trace resolved command voltage through release validation, staged execution, waveform generation, metadata, checkpoint/resume, and tests. Implement only the smallest correction that makes the executed command match the explicitly confirmed attenuation policy already represented by the code.

Focused test:

```text
matlab -nodisplay -nosplash -batch "cd(pwd); results = runtests('tests/TestAdaptiveOptopatch.m'); assertSuccess(results)"
```

Likely touch area: `+adaptive_optopatch/run_2p_manifest.m`, `+adaptive_optopatch/validate_2p_release_level.m`, and focused tests. Do not invent a new attenuation policy; stop if the existing interfaces do not specify one.
