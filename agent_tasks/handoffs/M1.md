# M1 — Missing physical DMD pattern-advance interval limit

Audit finding: no physical lower bound protects the externally triggered DMD pattern-advance interval. Locate the existing hardware capability/configuration that authoritatively defines this limit and enforce it during preflight or sequence preparation, before output is armed. Preserve the frozen event schedule and report a clear error that tells the operator what interval is invalid.

Focused test:

```text
matlab -nodisplay -nosplash -batch "cd(pwd); results = runtests('tests/TestBlueMaskEventExecutability.m'); assertSuccess(results)"
```

Likely touch area: DMD sequence preparation, preflight, hardware profile/configuration, and focused tests. Do not guess a physical timing constant. If no authoritative value or capability exists, stop and report the required policy decision rather than inventing one.
